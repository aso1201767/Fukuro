<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>
    
    
     <style name="ActionBarTabs" parent="@android:style/Widget.Holo.Light.ActionBar.TabView">
        <item name="android:background">@drawable/actionbar_tab_indicator</item>
        <!-- API level 10以下用 -->
        
    </style>

	<style name="CustomActionBarTheme" parent="@android:style/Theme.Holo.Light">
		<item name="android:actionBarStyle" >@style/MyActionBar</item>
		<item name="android:actionBarDivider">@null</item>
		<item name="android:actionBarTabStyle">@style/ActionBarTabs</item>
		<!-- <item name="android:background">@color/actionbar_background</item> -->
	</style>
	
	<!-- ActionBar styles -->
    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:titleTextStyle" >@style/MyActionBarTitleText</item>
        <item name="android:background">@color/actionbar_background</item>
        <item name="android:actionBarTabStyle">@style/MyActionBarTabs</item>
        <item name="android:actionBarTabBarStyle">@style/customActionBarTabBarStyle</item>
        <item name="android:backgroundSplit">@color/actionbar_background</item>
        <item name="android:backgroundStacked">@color/actionbar_background2</item>
        <!-- Support library compatibility -->
    </style>
    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/actionbar_text</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>
 
   <!-- ActionBar tabs styles -->
    <style name="MyActionBarTabs"
           parent="@android:style/Widget.Holo.Light.ActionBar.TabView">
        <!-- tab indicator -->
        <item name="android:paddingLeft">30dp</item>
        <item name="android:paddingRight">30dp</item>
                
    </style>    
        <!-- style for the tab bar -->
    <style name="customActionBarTabBarStyle">
        <item name="android:background">@color/black</item>
    </style>
    
    
    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
     <style name="eraser_radio" parent="android:Widget.DropDownItem">
        <item name="android:checkMark">@drawable/eraser_radio</item>
    </style>
    <style name="cutting_radio" parent="android:Widget.DropDownItem">
        <item name="android:checkMark">@drawable/cutting_radio</item>
    </style>
<!--     
    <style>
    </style>
 -->    
    <color name="action_bar_background">@color/black</color> <!-- この行を追加 -->

</resources>
